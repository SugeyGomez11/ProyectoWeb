# servicios/apps.py
from django.apps import AppConfig

# Declaración de la clase para la configuracion de la aplicación de servicios
class ServiciosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'servicios'
